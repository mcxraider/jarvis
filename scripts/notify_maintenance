#!/usr/bin/env bash

set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$script_dir/_broadcast_onboarded_telegram_users.sh" \
  "Jarvis is currently under maintenance. I’ll report back when it’s live again."
