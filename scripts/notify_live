#!/usr/bin/env bash

set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$script_dir/_broadcast_onboarded_telegram_users.sh" \
  "Jarvis is live again and I got smarter now. Have a productive day!" \
