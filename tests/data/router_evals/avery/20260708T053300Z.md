# Router evaluation - 2026-07-08T05:33:00Z

**Model:** deepseek-v4-flash - **Users:** 1 - **Queries:** 28 - **Pairs:** 28

---

## avery
Task provider: todoist - Event provider: todoist - calendar_usage: default - Active: todoist, google_calendar

### Query 1
**User query:**
> help me find out what projects i have in todoist, ill ask u to add a task to a one after that

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
help me find out what projects i have in todoist, ill ask u to add a task to a one after that
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "project listing query",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 1652 ms

### Query 2
**User query:**
> when am i free next week?

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
when am i free next week?
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "free/busy query",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 875 ms

### Query 3
**User query:**
> Go through my schedule, check everything that does not have a time, that is also not a birthday. Tell me first and then I will ask you to make edits

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
Go through my schedule, check everything that does not have a time, that is also not a birthday. Tell me first and then I will ask you to make edits
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "schedule query for untimed items",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 891 ms

### Query 4
**User query:**
> put in my cal

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
put in my cal
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "calendar item creation",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 923 ms

### Query 5
**User query:**
> yo whats up with my tasks today anything urgent

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
yo whats up with my tasks today anything urgent
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "tasks query",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 1058 ms

### Query 6
**User query:**
> remind me to text mom back later today

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
remind me to text mom back later today
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "task/reminder request",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 958 ms

### Query 7
**User query:**
> remind me tomorrow to submit the internship form

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
remind me tomorrow to submit the internship form
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "reminder/task request",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 1106 ms

### Query 8
**User query:**
> remind me next week to renew my library books

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
remind me next week to renew my library books
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "reminder task creation",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 902 ms

### Query 9
**User query:**
> can u add "buy protein powder" to my grocery project

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
can u add "buy protein powder" to my grocery project
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "add task to project",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 851 ms

### Query 10
**User query:**
> hey u there

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
hey u there
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [],
  "reasoning": "greeting",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 809 ms

### Query 11
**User query:**
> check my google cal for thursday plz

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
check my google cal for thursday plz
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "google_calendar"
  ],
  "reasoning": "explicit google calendar mention",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 814 ms

### Query 12
**User query:**
> is there anything on gcal for me tmr morning

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
is there anything on gcal for me tmr morning
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "google_calendar"
  ],
  "reasoning": "explicit gcal mention",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 1882 ms

### Query 13
**User query:**
> block off 2hrs friday afternoon for deep work

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
block off 2hrs friday afternoon for deep work
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "schedule block request",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 768 ms

### Query 14
**User query:**
> what do i have going on tmr, tasks and meetings both

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
what do i have going on tmr, tasks and meetings both
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist",
    "google_calendar"
  ],
  "reasoning": "asks for tasks and meetings",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 1083 ms

### Query 15
**User query:**
> move my dentist thing to next tuesday same time

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
move my dentist thing to next tuesday same time
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "schedule item change",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 927 ms

### Query 16
**User query:**
> dont add this to my calendar just keep it on my todo list, dentist appointment reminder

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
dont add this to my calendar just keep it on my todo list, dentist appointment reminder
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "user specifies todo list",
  "rewritten_query": "dentist appointment reminder",
  "uncertain": false
}
```
**Elapsed:** 915 ms

### Query 17
**User query:**
> dont put this on my calendar, remind me to call the clinic tomorrow

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
dont put this on my calendar, remind me to call the clinic tomorrow
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "reminder task",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 791 ms

### Query 18
**User query:**
> check my notion page for meeting notes from yesterday

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
check my notion page for meeting notes from yesterday
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [],
  "reasoning": "Request mentions Notion which is not a supported domain.",
  "rewritten_query": null,
  "uncertain": true
}
```
**Elapsed:** 954 ms

### Query 19
**User query:**
> check my notion schedule for tomorrow

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
check my notion schedule for tomorrow
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "schedule query",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 877 ms

### Query 20
**User query:**
> how many tasks are overdue rn

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
how many tasks are overdue rn
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "task query",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 767 ms

### Query 21
**User query:**
> can u check my email for the invoice from the plumber

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
can u check my email for the invoice from the plumber
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [],
  "reasoning": "request not supported",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 841 ms

### Query 22
**User query:**
> search gmail for next week's meeting invite

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
search gmail for next week's meeting invite
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "google_calendar"
  ],
  "reasoning": "calendar event query",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 1028 ms

### Query 23
**User query:**
> check slack for tomorrow's deployment schedule

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
check slack for tomorrow's deployment schedule
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [],
  "reasoning": "request out of scope",
  "rewritten_query": null,
  "uncertain": true
}
```
**Elapsed:** 977 ms

### Query 24
**User query:**
> look in google docs for tomorrow's plan

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
look in google docs for tomorrow's plan
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "google_calendar"
  ],
  "reasoning": "google docs is not an available domain; closest is calendar",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 1097 ms

### Query 25
**User query:**
> set my todoist priority for the essay task to p1

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
set my todoist priority for the essay task to p1
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "assign priority to todoist task",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 845 ms

### Query 26
**User query:**
> lol nvm forget what i just said

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
lol nvm forget what i just said
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [],
  "reasoning": "user canceled request",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 914 ms

### Query 27
**User query:**
> what all do i need to get done before i fly out saturday, check both my todos and whats already booked on my cal

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
what all do i need to get done before i fly out saturday, check both my todos and whats already booked on my cal
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist",
    "google_calendar"
  ],
  "reasoning": "explicitly mentions todos and calendar",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 1076 ms

### Query 28
**User query:**
> im free thursday and friday this wk which one should zac and i grab dinner

**Prompt (system):**
```text
You are a fast query router for a personal assistant. Classify which service domains the user's request needs. Do not answer the request or call any tools — only classify.

## Domains
- "todoist" (Todoist): tasks, projects, comments
- "google_calendar" (Google Calendar): calendar_events, free_busy

## Connection status
- Todoist: connected
- Google Calendar: connected

## Routing rules
1. Route tasks, to-dos, and projects to `todoist`.
2. Route events, schedules, availability, free-time, and busy-time requests to `todoist`.
3. Treat Todoist as able to answer scheduled-item, event, availability, and free/busy questions — generic calendar or schedule requests route to `todoist`.
4. Greetings, small talk, and meta questions return an empty `domains` list.
5. Multi-domain requests: return every domain the request touches.
6. Prefer connected domains, but if the request clearly needs a disconnected domain, still return it so the assistant can explain.

## Examples
User: "what tasks do I have today?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "task lookup"}
User: "what's on my schedule this week?" -> {"domains": ["todoist"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "schedule query"}
User: "hello!" -> {"domains": [], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "greeting"}
User: "add a meeting to my google calendar" -> {"domains": ["google_calendar"], "uncertain": false, "candidate_domains": [], "rewritten_query": null, "reasoning": "explicit google calendar mention"}

## Rewrite rules
1. If the request is already clear, set `rewritten_query` to null.
2. A rewrite must preserve timing modifiers such as `later today`, `tomorrow`, `next week`, time ranges, and recurrence hints.
3. Do not turn recommendations, comparisons, or planning requests into pure lookups.
4. Do not add missing task/event details, names, locations, attendees, durations, or dates.
5. Preserve uncertainty and wording strength: do not turn `maybe`, `could`, or `which one should` into a definite action.

## Output format
Return exactly one JSON object. No prose, no code fences.
Schema: {
  "domains": [<subset of "todoist", "google_calendar"> — most-likely minimal route],
  "uncertain": <boolean — true only for real domain ambiguity>,
  "candidate_domains": [<subset of "todoist", "google_calendar"> — expanded safe set when uncertain, else []],
  "rewritten_query": <string or null — faithful restatement, or null if already clear>,
  "reasoning": <short string, 10 words or fewer>
}
```

**Prompt (user):**
```text
User request:
im free thursday and friday this wk which one should zac and i grab dinner
```

**Response (raw RouterDecision):**
```json
{
  "candidate_domains": [],
  "domains": [
    "todoist"
  ],
  "reasoning": "availability query for schedule",
  "rewritten_query": null,
  "uncertain": false
}
```
**Elapsed:** 883 ms
